/* Ver.22.4 試合会場モード実戦完成版 */
(function(){
'use strict';
const KEY='furugen_live_match_v224', PAGE='live24', MODAL='live24Modal';
const CATS=['U-12','U-11','U-10','U-9'];
let S=null,timer=null;
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const now=()=>Date.now();
const today=()=>{const d=new Date(),p=n=>String(n).padStart(2,'0');return `${d.getFullYear()}-${p(d.getMonth()+1)}-${p(d.getDate())}`};
const isCoach=()=>{try{return typeof isStaff==='function'&&isStaff()}catch(_){return false}};
const msg=(t,type='warn')=>{try{typeof showMessage==='function'?showMessage(t,type):alert(t)}catch(_){alert(t)}};
function normCat(v){const s=String(v||'').toUpperCase().replace(/\s/g,'');if(['U-12','U12','6年','6年生','小学6年'].includes(s))return'U-12';if(['U-11','U11','5年','5年生','小学5年'].includes(s))return'U-11';if(['U-10','U10','4年','4年生','小学4年'].includes(s))return'U-10';if(['U-9','U9','3年','3年生','小学3年'].includes(s))return'U-9';return'未設定'}
function roster(){try{const src=(typeof players!=='undefined'&&Array.isArray(players))?players:(Array.isArray(window.players)?window.players:[]);return src.map((p,i)=>({id:String(p.id||p.player_id||p.uuid||i),name:String(p.name||p.player_name||p.full_name||'').trim(),category:normCat(p.grade||p.category||p.age_category||p.school_year),position:String(p.position||p.positions||p.role||'未設定'),photo:String(p.photo_url||p.image_url||p.avatar_url||p.photo||p.image||''),status:String(p.status||'現役')})).filter(p=>p.name&&['現役','active','在籍','所属',''].includes(p.status))}catch(e){console.error(e);return[]}}
function eligible(cat){const ci=CATS.indexOf(cat);return roster().filter(p=>{const pi=CATS.indexOf(p.category);return pi===ci||pi>ci}).sort((a,b)=>CATS.indexOf(a.category)-CATS.indexOf(b.category)||a.name.localeCompare(b.name,'ja'))}
function load(){try{S=JSON.parse(localStorage.getItem(KEY)||'null')}catch(_){S=null}}
function save(){try{S?localStorage.setItem(KEY,JSON.stringify(S)):localStorage.removeItem(KEY)}catch(e){console.error(e)}}
function elapsed(){return !S?0:Number(S.elapsedMs||0)+(S.running?Math.max(0,now()-Number(S.runStartedAt||now())):0)}
function fmt(ms){const n=Math.max(0,Math.floor(ms/1000));return `${String(Math.floor(n/60)).padStart(2,'0')}:${String(n%60).padStart(2,'0')}`}
function ensure(){let nav=$('live24Nav');if(!nav){nav=document.createElement('button');nav.id='live24Nav';nav.textContent='🏟 試合会場';nav.onclick=open;const n=document.querySelector('nav');if(n){const login=[...n.querySelectorAll('button')].find(b=>b.textContent.includes('コーチログイン'));n.insertBefore(nav,login||null)}}nav?.classList.toggle('coach-only-hidden',!isCoach());let p=$(PAGE);if(!p){p=document.createElement('section');p.id=PAGE;p.className='page';document.querySelector('main')?.appendChild(p)}if(!$(MODAL)){const m=document.createElement('div');m.id=MODAL;m.className='live24-modal hidden';document.body.appendChild(m)}}
function open(){ensure();if(!isCoach())return msg('試合会場モードはコーチログイン後に利用できます。');if(typeof showPage==='function')showPage(PAGE);render()}
function progress(step){return `<div class="live24-progress">${['試合情報','スタメン','開始前確認','ライブ記録','成績保存'].map((x,i)=>`<span class="${i<=step?'on':''}">${i+1}. ${x}</span>`).join('')}</div>`}
function avatar(p){return p.photo?`<img src="${esc(p.photo)}" alt="">`:`<span class="live24-avatar">👤</span>`}
function setup(){return `<div class="live24-card"><header><div><small>🏟 MATCH DAY LIVE</small><h2>Ver.22.4 試合会場モード実戦完成版</h2></div><b>${today()}</b></header><div class="live24-body">${progress(0)}<div class="live24-grid"><label>カテゴリー<select id="v24cat">${CATS.map(x=>`<option>${x}</option>`).join('')}</select></label><label>対戦相手<input id="v24opp" placeholder="例：MOSTRO"></label><label>大会名<input id="v24comp" placeholder="例：TRM"></label><label>会場<input id="v24venue" placeholder="例：古堅南小学校"></label><label>人数制<select id="v24count"><option value="8">8人制</option><option value="11">11人制</option><option value="custom">自由人数</option></select></label><label id="v24customCountWrap" class="hidden">人数<input id="v24customCount" type="number" min="1" max="18" value="8"></label><label>試合時間<select id="v24format"><option value="single-15">15分1本</option><option value="single-20" selected>20分1本</option><option value="single-25">25分1本</option><option value="half-15">15分ハーフ</option><option value="half-20">20分ハーフ</option><option value="custom-single">自由入力・1本</option><option value="custom-half">自由入力・前後半</option></select></label><label id="v24customMinutesWrap" class="hidden">1本／1ハーフ（分）<input id="v24minutes" type="number" min="1" max="90" value="20"></label></div><label class="live24-checkline"><input id="v24auto" type="checkbox" checked> 試合終了後、通常の試合入力へ転記して保存する</label><div class="live24-actions"><button class="primary" data-v24="select">スタメン選択へ</button>${S?'<button class="danger" data-v24="reset">途中記録を削除</button>':''}</div></div></div>`}
function count(){const v=$('v24count')?.value||'8';return v==='custom'?Math.max(1,Math.min(18,Number($('v24customCount')?.value||8))):Number(v)}
function format(){const v=$('v24format')?.value||'single-20',c=Math.max(1,Math.min(90,Number($('v24minutes')?.value||20)));const m={'single-15':['single',15,'15分1本'],'single-20':['single',20,'20分1本'],'single-25':['single',25,'25分1本'],'half-15':['half',15,'15分ハーフ'],'half-20':['half',20,'20分ハーフ'],'custom-single':['single',c,`${c}分1本`],'custom-half':['half',c,`${c}分ハーフ`]};return m[v]||m['single-20']}
function beginSelection(){const cat=$('v24cat')?.value,opp=$('v24opp')?.value.trim(),comp=$('v24comp')?.value.trim(),venue=$('v24venue')?.value.trim();if(!opp)return msg('対戦相手を入力してください。');const [type,mins,label]=format(),starterCount=count(),list=eligible(cat);if(list.length<starterCount)return msg(`候補選手が${list.length}名です。スタメン${starterCount}名を選べません。`);S={version:'22.4',phase:'selection',date:today(),category:cat,opponent:opp,competition:comp,venue,matchType:type,periodMinutes:mins,formatLabel:label,starterCount,autoSave:!!$('v24auto')?.checked,period:type==='half'?'前半':'1本',periodNo:1,started:false,running:false,runStartedAt:null,elapsedMs:0,totalMatchMs:0,gf:0,ga:0,lineup:{},stats:{},events:[]};list.forEach(p=>{S.lineup[p.id]=false;S.stats[p.id]={...p,played:false,activeMs:0,onAt:null,goals:0,assists:0,yellow:0,red:0}});save();render()}
function selection(){const ids=Object.keys(S.stats),chosen=ids.filter(id=>S.lineup[id]);return `<div class="live24-card"><header><div><small>STEP 2 / スタメン選択</small><h2>${esc(S.category)} vs ${esc(S.opponent)}</h2></div><b>${chosen.length}/${S.starterCount}名</b></header><div class="live24-body">${progress(1)}<div class="live24-select-grid">${ids.map(id=>{const p=S.stats[id],on=S.lineup[id];return `<button class="live24-select-player ${on?'selected':''}" data-v24player="${esc(id)}">${avatar(p)}<span><b>${esc(p.name)}</b><small>${esc(p.category)} / ${esc(p.position)}</small></span><em>${on?'✓ 選択':'未選択'}</em></button>`}).join('')}</div><div class="live24-sticky"><strong>選択 ${chosen.length} / ${S.starterCount}名</strong><div class="live24-actions"><button data-v24="back">戻る</button><button class="primary" data-v24="confirm" ${chosen.length===S.starterCount?'':'disabled'}>開始前確認へ</button></div></div></div></div>`}
function line(id,kind){const p=S.stats[id];return `<div class="live24-player ${kind}">${avatar(p)}<span><b>${esc(p.name)}</b><small>${esc(p.category)} / ${esc(p.position)}</small></span>${kind==='live'?`<button data-v24sub="${esc(id)}">🔄 交代</button>`:''}</div>`}
function ready(){const on=Object.keys(S.lineup).filter(id=>S.lineup[id]),off=Object.keys(S.lineup).filter(id=>!S.lineup[id]);return `<div class="live24-card"><header><div><small>STEP 3 / 試合開始前確認</small><h2>${esc(S.category)} vs ${esc(S.opponent)}</h2></div><b>${esc(S.formatLabel)}</b></header><div class="live24-body">${progress(2)}<div class="live24-ready-banner">タイマーは停止中です。「▶ 試合開始」を押した瞬間から計測します。</div><div class="live24-summary"><div>大会<b>${esc(S.competition||'未入力')}</b></div><div>会場<b>${esc(S.venue||'未入力')}</b></div><div>スタメン<b>${S.starterCount}名</b></div><div>形式<b>${esc(S.formatLabel)}</b></div></div><div class="live24-rosters"><section><h3>スタメン</h3>${on.map(id=>line(id,'starter')).join('')}</section><section><h3>ベンチ</h3>${off.map(id=>line(id,'bench')).join('')||'<p>ベンチなし</p>'}</section></div><div class="live24-actions center"><button data-v24="edit">スタメンを変更</button><button class="kickoff" data-v24="kickoff">▶ ${S.period==='後半'?'後半開始':'試合開始'}</button><button class="danger" data-v24="reset">試合を取消</button></div></div></div>`}
function live(){const on=Object.keys(S.lineup).filter(id=>S.lineup[id]),off=Object.keys(S.lineup).filter(id=>!S.lineup[id]),e=elapsed(),limit=S.periodMinutes*60000,over=e>=limit;return `<div class="live24-card live"><header><div><small>🔴 LIVE MATCH</small><h2>${esc(S.category)} vs ${esc(S.opponent)}</h2></div><b>${esc(S.period)} / ${S.running?'進行中':'一時停止'}</b></header><div class="live24-body">${progress(3)}${over?'<div class="live24-warning live24-timer-over">設定時間を超えています。必要に応じて試合終了または前半終了を押してください。</div>':''}<div class="live24-score"><div><b>古堅南FC</b><strong>${S.gf}</strong></div><div><span>${fmt(e)}</span><small>目安 ${fmt(limit)}</small></div><div><b>${esc(S.opponent)}</b><strong>${S.ga}</strong></div></div><div class="live24-controls"><button data-v24="toggle">${S.running?'⏸ 一時停止':'▶ 再開'}</button>${S.matchType==='half'&&S.period==='前半'?'<button data-v24="halftime">⏹ 前半終了</button>':''}<button class="danger" data-v24="finish">■ 試合終了</button></div><div class="live24-event-buttons"><button class="goal" data-v24event="goal">⚽<span>得点</span></button><button class="assist" data-v24event="assist">🎯<span>アシスト</span></button><button class="opp" data-v24event="oppgoal">🥅<span>相手得点</span></button><button class="yellow" data-v24event="yellow">🟨<span>警告</span></button><button class="red" data-v24event="red">🟥<span>退場</span></button></div><div class="live24-rosters live-grid"><section><h3>出場中 ${on.length}名</h3>${on.map(id=>line(id,'live')).join('')}</section><section><h3>ベンチ ${off.length}名</h3>${off.map(id=>line(id,'bench')).join('')||'<p>ベンチなし</p>'}</section><section class="timeline"><h3>記録タイムライン</h3>${S.events.map(x=>`<div><b>${esc(x.time)}</b><span>${esc(x.text)}</span></div>`).join('')||'<p>まだ記録はありません。</p>'}</section></div></div></div>`}
function saveView(){return `<div class="live24-card"><header><div><small>STEP 5 / 成績保存</small><h2>試合記録の確認</h2></div><b>${S.gf} - ${S.ga}</b></header><div class="live24-body">${progress(4)}<div class="live24-save-result"><h3>古堅南FC ${S.gf} - ${S.ga} ${esc(S.opponent)}</h3><p>${esc(S.date)} / ${esc(S.category)} / ${esc(S.formatLabel)}</p><p>記録 ${S.events.length}件・出場 ${Object.values(S.stats).filter(x=>x.played).length}名</p></div><div class="live24-actions center"><button class="primary" data-v24="transfer">通常の試合入力へ転記</button><button data-v24="resume-finished">ライブ画面へ戻る</button><button class="danger" data-v24="reset">記録を破棄</button></div></div></div>`}
function render(){ensure();load();const p=$(PAGE);if(!p)return;if(!S)p.innerHTML=setup();else if(S.phase==='selection')p.innerHTML=selection();else if(S.phase==='ready')p.innerHTML=ready();else if(S.phase==='live')p.innerHTML=live();else if(S.phase==='save')p.innerHTML=saveView();else p.innerHTML=setup()}
function togglePlayer(id){if(!S||S.phase!=='selection'||!(id in S.lineup))return;const n=Object.values(S.lineup).filter(Boolean).length;if(!S.lineup[id]&&n>=S.starterCount)return msg(`スタメンは${S.starterCount}名までです。`);S.lineup[id]=!S.lineup[id];save();render()}
function confirmLineup(){const n=Object.values(S.lineup).filter(Boolean).length;if(n!==S.starterCount)return msg(`スタメンを${S.starterCount}名選択してください。`);Object.keys(S.stats).forEach(id=>S.stats[id].played=!!S.lineup[id]);S.phase='ready';save();render();msg('スタメンを確定しました。試合開始ボタンを押してください。','ok')}
function kickoff(){if(!S||S.phase!=='ready')return;S.phase='live';S.started=true;S.running=true;S.runStartedAt=now();Object.keys(S.lineup).forEach(id=>{if(S.lineup[id]){S.stats[id].played=true;S.stats[id].onAt=now()}});S.events.unshift({time:'00:00',text:`${S.period}開始`});save();render()}
function pause(){if(!S?.running)return;const n=now();S.elapsedMs=elapsed();S.totalMatchMs=(S.totalMatchMs||0)+(n-Number(S.runStartedAt||n));S.running=false;S.runStartedAt=null;Object.keys(S.lineup).forEach(id=>{const p=S.stats[id];if(S.lineup[id]&&p.onAt){p.activeMs+=n-p.onAt;p.onAt=null}});save()}
function resume(){if(!S||S.phase!=='live'||S.running)return;S.running=true;S.runStartedAt=now();Object.keys(S.lineup).forEach(id=>{if(S.lineup[id])S.stats[id].onAt=now()});save();render()}
function halftime(){if(!S||S.matchType!=='half'||S.period!=='前半')return;pause();S.events.unshift({time:fmt(S.elapsedMs),text:'前半終了'});S.period='後半';S.periodNo=2;S.phase='ready';S.elapsedMs=0;save();render();msg('前半終了です。後半開始ボタンを押すまでタイマーは止まっています。','ok')}
function modal(content){const m=$(MODAL);m.innerHTML=`<div class="live24-modal-box">${content}</div>`;m.classList.remove('hidden')}
function close(){ $(MODAL)?.classList.add('hidden') }
function pick(title,ids,cb){modal(`<div class="live24-modal-head"><h2>${esc(title)}</h2><button data-v24="close">閉じる</button></div><div class="live24-picks">${ids.map(id=>`<button data-v24pick="${esc(id)}">${avatar(S.stats[id])}<span><b>${esc(S.stats[id].name)}</b><small>${esc(S.stats[id].category)} / ${esc(S.stats[id].position)}</small></span></button>`).join('')||'<p>対象選手がいません。</p>'}</div>`);$(MODAL)._cb=cb}
function addEvent(type){if(!S||S.phase!=='live')return;const t=fmt(elapsed());if(type==='oppgoal'){S.ga++;S.events.unshift({time:t,text:'相手チーム得点'});save();render();return}const ids=Object.keys(S.lineup).filter(id=>S.lineup[id]),labels={goal:'得点',assist:'アシスト',yellow:'警告',red:'退場'};pick(`${labels[type]}した選手`,ids,id=>{const p=S.stats[id];if(type==='goal'){p.goals++;S.gf++}else if(type==='assist')p.assists++;else if(type==='yellow')p.yellow++;else if(type==='red'){p.red++; if(p.onAt){p.activeMs+=now()-p.onAt;p.onAt=null}S.lineup[id]=false}S.events.unshift({time:fmt(elapsed()),text:`${p.name}：${labels[type]}`});save();render()})}
function sub(outId){if(!S||S.phase!=='live')return;const bench=Object.keys(S.lineup).filter(id=>!S.lineup[id]&&S.stats[id].red===0);pick(`IN選手を選択（OUT ${S.stats[outId].name}）`,bench,inId=>{const n=now(),out=S.stats[outId],inn=S.stats[inId];if(out.onAt){out.activeMs+=n-out.onAt;out.onAt=null}S.lineup[outId]=false;S.lineup[inId]=true;inn.played=true;inn.onAt=n;S.events.unshift({time:fmt(elapsed()),text:`交代 OUT ${out.name} → IN ${inn.name}`});save();render()})}
function finish(){if(!S)return;pause();S.phase='save';save();render()}
function reset(){if(S?.running)pause();S=null;save();close();render()}
function setValue(id,v){const e=$(id);if(!e)return;e.value=v;e.dispatchEvent(new Event('change',{bubbles:true}))}
async function transfer(){const F=JSON.parse(JSON.stringify(S));close();if(typeof showPage==='function')showPage('entry');setTimeout(()=>{setValue('matchDate',F.date);setValue('matchCategory',F.category);setValue('competition',F.competition);setValue('opponent',F.opponent);setValue('venue',F.venue);setValue('goalsFor',F.gf);setValue('goalsAgainst',F.ga);setValue('matchMemo',`Ver.22.4 試合会場モード\n試合形式：${F.formatLabel}\nスタメン：${F.starterCount}名\n${F.events.slice().reverse().map(e=>`${e.time} ${e.text}`).join('\n')}`);try{typeof renderRecordInputs==='function'&&renderRecordInputs()}catch(e){console.error(e)}setTimeout(async()=>{document.querySelectorAll('.player-entry[data-player]').forEach(row=>{const p=F.stats[String(row.dataset.player)];if(!p)return;const q=c=>row.querySelector(c);if(q('.played'))q('.played').checked=!!p.played;if(q('.minutes'))q('.minutes').value=Math.max(0,Math.round((p.activeMs||0)/60000));if(q('.goals'))q('.goals').value=p.goals||0;if(q('.assists'))q('.assists').value=p.assists||0;if(q('.yellow'))q('.yellow').value=p.yellow||0;if(q('.red'))q('.red').value=p.red||0});if(F.autoSave&&typeof saveMatchWithRecords==='function'){try{await saveMatchWithRecords();S=null;save();msg('試合結果と選手成績を保存しました。','ok')}catch(e){console.error(e);msg('自動保存できませんでした。内容を確認して保存してください。')}}else msg('内容を確認して「試合を保存」を押してください。','ok')},500)},250)}
document.addEventListener('click',e=>{const t=e.target.closest('[data-v24],[data-v24player],[data-v24event],[data-v24sub],[data-v24pick]');if(!t)return;const a=t.dataset.v24;if(a==='select')beginSelection();else if(a==='back')reset();else if(a==='confirm')confirmLineup();else if(a==='edit'){S.phase='selection';save();render()}else if(a==='kickoff')kickoff();else if(a==='toggle')S.running?(pause(),render()):resume();else if(a==='halftime')halftime();else if(a==='finish')finish();else if(a==='transfer')transfer();else if(a==='resume-finished'){S.phase='live';save();render()}else if(a==='reset'){if(confirm('この試合の記録を削除しますか？'))reset()}else if(a==='close')close();else if(t.dataset.v24player)togglePlayer(t.dataset.v24player);else if(t.dataset.v24event)addEvent(t.dataset.v24event);else if(t.dataset.v24sub)sub(t.dataset.v24sub);else if(t.dataset.v24pick){const cb=$(MODAL)._cb;close();typeof cb==='function'&&cb(t.dataset.v24pick)}});
document.addEventListener('change',e=>{if(e.target.id==='v24count')$('v24customCountWrap')?.classList.toggle('hidden',e.target.value!=='custom');if(e.target.id==='v24format')$('v24customMinutesWrap')?.classList.toggle('hidden',!e.target.value.startsWith('custom'))});
document.addEventListener('DOMContentLoaded',()=>{load();ensure();render();timer=setInterval(()=>{if(S?.phase==='live'&&S.running&&$(PAGE)?.classList.contains('show'))render()},1000)});window.addEventListener('pageshow',()=>{ensure();render()});window.openLive24=open;
})();
